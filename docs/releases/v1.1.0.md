# AI Multilingual — Release Notes v1.1.0

**Release version:** 1.1.0  
**Schema target:** 6  
**Prepared:** 2026-08-09  
**Package:** `ai-multilingual-1.1.0.zip`  
**Scope audit:** [V1_1_0_RELEASE_SCOPE.md](V1_1_0_RELEASE_SCOPE.md)

This is the **first intentional/public** release package. Historical `v1.0.0` tag/metadata exists in the repository but must not be treated as a completed public launch.

## Highlights

- Multilingual visitor experience for WordPress chrome and WooCommerce catalog / journey / customer emails
- Fluent Forms contact chrome via Integration API v1
- First-class SEO stack: canonical + hreflang, Rank Math title/meta overlays, Open Graph/Twitter text, sitemap language discovery (with honesty gates), and bounded SEO diagnostics
- Production ZIP is audited (runtime assets only; no tests/dev sources)
- Upgrade from a historical 1.0.0 package keeps schema target **6** (no migration bump)

## Compatibility

| Requirement | Value |
|---|---|
| WordPress | 6.5+ (tested up to 6.8) |
| PHP | 8.1+ |
| WooCommerce | 10.x recommended (dev/CI pin **10.9.4**) |
| Schema option `aiml_db_version` | **6** after activate / drift migrate |
| Rank Math | Optional foreign owner for title/meta/OG/sitemap hooks |

## SEO

**Implemented**

- Canonical URLs and hreflang (+ `x-default`) via SB11 relationships (A.SEOb)
- Rank Math document title / meta description overlays (A.SEOc)
- Open Graph / Twitter **text** overlays on official Rank Math hooks (A.SEOd)
- Rank Math sitemap `xhtml:link` enrichment when the site is public and multilingual (A.SEOe)
- Bounded SEO diagnostics: `wp aiml seo status` and admin presentation (A.SEOf)

**Honesty / partial**

- When `blog_public=0` (Discourage search engines), public discovery enrichment is suppressed
- SC7–SC9 and some OG/Twitter surfaces remain Partially Supported per validation logs
- SF15 readiness is advisory only — **no** Search Console API automation

## WordPress / WooCommerce coverage

**WordPress**

- Custom navigation menu item titles (A.6 Supported N1)

**WooCommerce**

- Product & catalog overlays (A.7a)
- Archive orderby label chrome (A.7b)
- Checkout / My Account journey chrome (A.7c Supported set)
- Customer email subject + heading overlays with order transactional language (A.7d / ADR-0018)

**Integrations**

- Fluent Forms contact bridge (A.8)

## Diagnostics / Operations

- `wp aiml seo status` — machine-readable bounded checks (not a full-site crawler)
- Health / jobs / rollout diagnostics from the platform track remain available
- Action Scheduler remains required for Background Translation Jobs
- Production install path: upload ZIP → activate → confirm `aiml_db_version = 6`

## Testing / Release Engineering

- Full CI gates: PHPCS (warnings fail), unit, integration (incl. PluginGuard), build + `bin/audit-zip.sh`
- Integration harness initializes Action Scheduler before WooCommerce install (test-only)
- Release workflow on `v*` tags: version match → production Composer install → build → audit → GitHub Release

## Known limitations

Still current (do not treat as shipped):

1. **Translated leaf slugs** (and uniqueness / URL history / rewrite bases) remain **Deferred** (A.SEOa SA1–SA6 / SA8–SA9). Prefixed URLs continue to use source slugs (e.g. `/sv/<source-slug>/`).
2. **Deferred social image/card surfaces** (A.SEOd SD4 / SD9 / SD10 / SD12).
3. **Deferred sitemap media / reusable SitemapDiscovery contract** (A.SEOe SE10 / SE11).
4. **`blog_public=0`** honestly blocks public SEO discovery enrichment.
5. **Pre-existing `/sv/` front-page 301 self-loop** may still reproduce on some hosts (detected by diagnostics as `error/self_loop`; not fixed in A.SEOf).
6. **Pre-existing duplicate product `<title>`** (Rank Math / theme ownership) may still reproduce; AIML does not add a second title tag.
7. **No Search Console API automation** (SF15 advisory only).
8. **Diagnostics are bounded** — not a site-wide crawler.
9. Platform carry-forwards: no Elementor body translation; no nested container block identity; render cache default-off; A.6/A.7 Deferred chrome/email/body surfaces per their validation logs.

## Upgrade from historical 1.0.0 packages

1. Install `ai-multilingual-1.1.0.zip` over the previous plugin directory (or replace via standard WP update).
2. Activate / visit wp-admin so `maybe_migrate()` runs.
3. Confirm `aiml_db_version` is **6** (unchanged target).
4. Smoke-check a published language front page, a product URL, Rank Math head output, and `wp aiml seo status`.

No destructive schema migration is introduced by 1.1.0.
