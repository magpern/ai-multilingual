# AI Multilingual — Release Notes v1.5.1

**Status:** RELEASE PREPARATION (not tagged; GitHub Release not created)  
**Release version:** 1.5.1  
**Schema target:** **8** (unchanged)  
**Prepared:** 2026-08-15  
**Package (release-prep):** `ai-multilingual-1.5.1.zip`  
**Previous release:** `v1.5.0` @ `03a3a09a7ee4e1a0d7624582dcfe07af90ce89d5`  
**Scope audit:** [V1_5_1_RELEASE_SCOPE.md](V1_5_1_RELEASE_SCOPE.md)  
**Preparation baseline:** [V1_5_1_PREPARATION_BASELINE.md](V1_5_1_PREPARATION_BASELINE.md)  
**Corrective closure:** [V151_LOCALIZED_URL_CORRECTNESS_STABILIZATION_CLOSURE.md](../plans/V151_LOCALIZED_URL_CORRECTNESS_STABILIZATION_CLOSURE.md)

This is a **patch corrective release**. It restores Supported Localized URL / SEO contracts that failed under Localized URLs ON during v1.5.0 Gate B dogfood. It does **not** add a new localized URL capability, SEO architecture, Woo permalink feature, Program B, or MSEO.6.

## Why this patch exists

Gate B on `dev.biopentra.eu` recorded:

- **D1:** CURRENT_LOCALIZED GET timeout / HTTP 500 from unbounded `term_link` re-entry  
- **D2:** hreflang emitting source-slug paths instead of EffectiveUrl  
- **D3a:** Woo/product `og:url` divergence in the same identity family  
- **D3b:** Truncated Woo HTML classified as the same D1 root-cause family  

Corrective implementation merged via PR #43 before this release preparation.

## Corrected Supported contracts

- Localized CURRENT_LOCALIZED page GET can complete (no AIML max-exec recursion loop)  
- `term_link` / `get_term_link` interaction bounded under LU ON  
- hreflang agrees with EffectiveUrl for discoverable objects with active routes  
- Open Graph URL / switcher agree when they share SB11 public identity  
- Woo localized PDP render health for the D1-adjacent breadcrumb/term_link path  
- Source routes, hierarchy, one-hop history, canonical, x-default, sitemap XHTML Model A, and LU OFF behavior remain within frozen architecture  

## Upgrade behavior (1.5.0 → 1.5.1)

1. Install `ai-multilingual-1.5.1.zip` over the previous plugin directory.  
2. Confirm `aiml_db_version` remains **8** (no migration).  
3. Existing active routes and history remain valid — no forced rematerialization.  
4. Settings and Localized URLs admission/defaults are unchanged.  
5. Preview remains source-slug only.  

## TARGET / schema / API

| Item | Status |
|---|---|
| `Migrator::TARGET` | **8** |
| Migration | **None** |
| Public Extension API | Unchanged |
| Public Integration API | Unchanged |
| New URL authority | **None** |

## Compatibility

Requires WordPress 6.5+, PHP 8.1+. WooCommerce optional; Rank Math coexistence unchanged. Sitemap Model A unchanged (default-language primary locs; localized XHTML alternates).

## Known limitations (honest)

- Translated rewrite bases / Woo endpoint names — Deferred  
- Variation-specific routes — Unsupported  
- Pretty layered-nav translation — Deferred  
- Extension API 1.1 URL observation — out of scope  
- Taxonomy route operator-completeness UI/API — out of scope  
- Program B / Jobs UX / Strategy F UX — not started  

## Acceptance after release

After a separately authorized tag + GitHub Release, **published-artifact DEV re-acceptance** on `dev.biopentra.eu` remains mandatory (V151AC22). This preparation does **not** deploy or accept.

## Deployment

**PRODUCTION DEPLOYMENT** is not part of this release preparation.  
**DEV deployment** is not part of this release preparation.
