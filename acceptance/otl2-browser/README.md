# OTL.2 browser suite — historical archive

This package is a **frozen historical archive** of the OTL.2 unified detail smoke.

The authoritative operator-lifecycle Playwright suite is
[`acceptance/otl-browser/`](../otl-browser/).

Do not treat this package as the current acceptance gate. Config fixes only;
do not rewrite historical product assertions.
